apiVersion: v1
kind: Service
metadata:
  name: {{ include "carbon-capture-network.fullname" . }}-ai-engine
  labels:
    app.kubernetes.io/name: {{ include "carbon-capture-network.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: ai-engine
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  {{- with .Values.aiEngine.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.aiEngine.service.type }}
  ports:
    - port: {{ .Values.aiEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "carbon-capture-network.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: ai-engine
